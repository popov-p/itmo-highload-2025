name: highload-2025-ci

on:
  push:
    branches:
      - "467160"
  pull_request:
    branches:
      - "467160"

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Echo Hello World
        run: echo "Hello World"
      # - name: Install backend dependencies
      #   uses: actions/setup-java@v3
      #   with:
      #     distribution: 'temurin'
      #     java-version: '21'
      
      # - name: Install Node.js
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: '18'
  
      # - name: Install frontend dependencies
      #   run: cd frontend && npm install

      # - name: Build backend
      #   run: cd backend/ && ./gradlew build -x test --no-daemon
      
      # - name: Build frontend
      #   run: cd frontend/ && npm run build --if-present

  # test:
  #   runs-on: ubuntu-22.04
  #   needs: build
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     - name: Install backend dependencies
  #       uses: actions/setup-java@v3
  #       with:
  #         distribution: 'temurin'
  #         java-version: '21'
      
  #     - name: Install Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '18'
  
  #     - name: Install frontend dependencies
  #       run: cd frontend && npm install

  #     - name: Tests backend
  #       run: cd backend/ && ./gradlew test --info --no-daemon

  #     - name: Tests frontend
  #       run: cd frontend && npm test -- --run

  # sonarqube_scan:
  #   name: sonar
  #   needs: test
  #   runs-on: ubuntu-22.04

  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     - name: Set up JDK 21
  #       uses: actions/setup-java@v3
  #       with:
  #         distribution: 'temurin'
  #         java-version: '21'


  #     - name: Install Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '18'
  
  #     - name: Install frontend dependencies
  #       run: cd frontend && npm install


  #     - name: SonarQube Scan Backend
  #       env:
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  #         SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
  #         SONAR_PROJECT_KEY_BACKEND: ${{ secrets.SONAR_PROJECT_KEY_BACKEND }}
  #         SONAR_ORGANIZATION_KEY: ${{ secrets.SONAR_ORGANIZATION_KEY }}
  #       run: |
  #         cd backend
  #         ./gradlew clean test jacocoTestReport --info -Dsonar.branch.name=master sonar

  #     - name: Check Coverage Percentage Backend
  #       run: |
  #         cd backend
  #         ./gradlew extractCoverage

  #     - name: SonarQube Scan Frontend
  #       env:
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  #         SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
  #         SONAR_PROJECT_KEY_FRONTEND: ${{ secrets.SONAR_PROJECT_KEY_FRONTEND }}
  #         SONAR_ORGANIZATION_KEY: ${{ secrets.SONAR_ORGANIZATION_KEY }}
  #       run: |
  #         cd frontend 
  #         npm run sonar:coverage
      
  #     - name: Check Coverage Percentage Frontend
  #       run: |
  #         cd frontend
  #         chmod +x ./check-coverage-frontend.sh && ./check-coverage-frontend.sh

  # docker_publish:
  #   name: publish
  #   needs: sonarqube_scan
  #   runs-on: ubuntu-22.04
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     - name: Set Docker tag
  #       id: vars
  #       run: echo "TAG=1.0.${{ github.run_number }}" >> $GITHUB_ENV
        
  #     - name: Build frontend
  #       run: |
  #         docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/devel-frontend:${{ env.TAG }} ./frontend
  #         docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/devel-frontend:latest ./frontend

  #     - name: Build backend
  #       run: |
  #         docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/devel-backend:${{ env.TAG }} ./backend
  #         docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/devel-backend:latest ./backend

  #     - name: Log in to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}

  #     - name: Push frontend
  #       run: |
  #         docker push ${{ secrets.DOCKERHUB_USERNAME }}/devel-frontend:${{ env.TAG }}
  #         docker push ${{ secrets.DOCKERHUB_USERNAME }}/devel-frontend:latest

  #     - name: Push backend
  #       run: |
  #         docker push ${{ secrets.DOCKERHUB_USERNAME }}/devel-backend:${{ env.TAG }}
  #         docker push ${{ secrets.DOCKERHUB_USERNAME }}/devel-backend:latest